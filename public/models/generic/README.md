# Generic Inspection Models

Place future body-type fallback models here:

- `coupe.glb`
- `hatchback.glb`
- `sedan.glb`
- `suv.glb`
- `pickup.glb`

GLB is preferred for web loading. Matching `.stl` files can be used as fallback only when a GLB does not exist.
